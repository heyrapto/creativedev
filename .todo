1. i should be able to use one element to hit the other one
2. if i drag an element, it shouldn't snap back until i realease it
3. the ring should be on top not inside the element. also the bth should have it